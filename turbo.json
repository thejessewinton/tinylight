{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["./packages/**/dist/**", ".next/**"]
    },
    "dev": {
      "cache": false
    }
  }
}
